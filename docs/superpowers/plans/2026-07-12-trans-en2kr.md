# Trans-EN2KR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trans-Complex2KR 기반으로, 고유명사를 영문판 기준으로 치환한 Trans-EN2KR 번역본을 생성한다.

**Architecture:** (1) Complex2KR를 EN2KR로 복사 → (2) josa 인식 결정론적 치환 엔진(Python, TDD)으로 고빈도·distinctive 고유명사 치환 → (3) 지명은 glossary 카탈로그에서 「」-delimited 리터럴 치환 생성 → (4) 汜(영)·복합 지명·잔여는 문맥 LLM 클린업 패스 → (5) 문서 재작성 → (6) 검증.

**Tech Stack:** Python 3.10 (stdlib만: json, re, unicodedata, pathlib), pytest.

## Global Constraints

- 치환 대상은 JSON **값(string)만**. 키(uid, "id:Prefix", locKey)는 절대 변경 금지.
- 플레이스홀더/태그 원형 보존: `{0}` `{1}` `@{0}@` `%s` `<color=...>` `</color>` `<p>` `#AVATAR:..#` `「」` `\n`.
- 확정 치환 맵은 spec `docs/superpowers/specs/2026-07-12-trans-en2kr-design.md` §2가 단일 권위. 충돌 시 spec 우선.
- 유지(치환 안 함): 별노래(Starsong), 메아리(Echo), 공진기, 공명, 크레딧, 광련, 토끼굴, 무녀, 군산, 意譯 지명(쓰레기산·실크 오아시스·심수·나루·남산), 이미 EN 음차(메이페어·마르세유·에펠·샘·리사·로댕·시라츠카·노미 등), 게임 타이틀 「별노래의 메아리」.
- `汜(영)` 계열은 결정론적 일괄 치환 **금지**(영혼·촬영기 등 substring 충돌). 안전한 다어절 시설명 리터럴 + LLM 패스로만.
- Windows/Git Bash 환경. 파일 인코딩 UTF-8. JSON 저장 시 `ensure_ascii=False`, `indent` 원형 유지 확인.
- 커밋은 각 Task 끝에서. 브랜치는 현재 `dev/2.23.0-...` 유지.

---

## 파일 구조

```
Trans-EN2KR/
  translations/*.json      # Complex2KR에서 복사 후 치환
  characters/*.{json,md}   # 복사; uid2speaker/actor_defs는 CHT기반=무변경, *.md는 치환
  glossary/*.{json,md}     # 재작성
  decisions/*.md           # 재작성
  README.md                # 재작성
  review/                  # 복사(유지)
  build/                   # 기존 유지(범위 밖)
tools/en2kr/
  josa.py                  # 조사 종성 교정 코어
  substitute.py            # 맵 적용 엔진
  substitution_map.json    # 세계관·신명·인물 확정 맵(auto/literal)
  gen_places_map.py        # glossary 지명→「」리터럴 치환 생성
  places_map.json          # (생성물)
  apply.py                 # 파일 변환 CLI
  verify.py                # 검증 CLI
  tests/
    test_josa.py
    test_substitute.py
```

---

### Task 1: EN2KR 스캐폴딩 (Complex2KR 복사)

**Files:**
- Create(copy): `Trans-EN2KR/translations/`, `Trans-EN2KR/characters/`, `Trans-EN2KR/glossary/`, `Trans-EN2KR/decisions/`, `Trans-EN2KR/review/`, `Trans-EN2KR/README.md`
- Create: `tools/en2kr/` (빈 디렉터리)

- [ ] **Step 1: 콘텐츠 디렉터리 복사** (build/는 제외 — 이미 존재하고 범위 밖)

```bash
cd "D:/my_projects/OPUS-Echo-Of-Starsong-Korean"
for d in translations characters glossary decisions review; do
  mkdir -p "Trans-EN2KR/$d"
  cp -r "Trans-Complex2KR/$d/." "Trans-EN2KR/$d/"
done
cp "Trans-Complex2KR/README.md" "Trans-EN2KR/README.md"
mkdir -p tools/en2kr/tests
# 패키지 임포트(from tools.en2kr...)를 위해 __init__.py 생성
touch tools/__init__.py tools/en2kr/__init__.py tools/en2kr/tests/__init__.py
```

> 모든 `python`·`pytest` 실행은 **repo 루트에서**. 스크립트 실행은 모듈 형식(`python -m tools.en2kr.apply`)으로 — `tools.en2kr.*` 상대 임포트가 동작하도록.

- [ ] **Step 2: 복사 검증 (파일 수 일치 + 전 JSON 파싱)**

```bash
python - <<'PY'
import json, glob, sys
n=0
for f in glob.glob("Trans-EN2KR/**/*.json", recursive=True):
    json.load(open(f, encoding="utf-8")); n+=1
print("json ok:", n)
import os
for d in ["translations","characters","glossary","decisions"]:
    a=len(os.listdir(f"Trans-Complex2KR/{d}")); b=len(os.listdir(f"Trans-EN2KR/{d}"))
    assert a==b, (d,a,b)
print("counts match")
PY
```
Expected: `json ok: N` (≥19), `counts match`. 실패 시 복사 재실행.

- [ ] **Step 3: Commit**

```bash
git add Trans-EN2KR tools/en2kr/.gitkeep 2>/dev/null; touch tools/en2kr/.gitkeep; git add -A Trans-EN2KR tools/en2kr
git commit -q -m "feat(en2kr): Complex2KR 콘텐츠 복사로 EN2KR 스캐폴딩"
```

---

### Task 2: josa.py — 조사 종성 교정 코어 (TDD)

**Files:**
- Create: `tools/en2kr/josa.py`
- Test: `tools/en2kr/tests/test_josa.py`

**Interfaces:**
- Produces:
  - `has_batchim(word: str) -> bool` — 마지막 한글 음절의 받침 유무. 한글 아님/빈문자열이면 False.
  - `is_rieul(word: str) -> bool` — 마지막 음절 받침이 ㄹ인지.
  - `josa_variants(old: str, new: str) -> list[tuple[str,str]]` — old에 붙던 조사 표면형과 new의 교정 표면형 쌍 목록. old/new 종성이 같으면 표면형 동일. 최장 조사부터 정렬됨.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tools/en2kr/tests/test_josa.py
from tools.en2kr.josa import has_batchim, is_rieul, josa_variants

def test_batchim():
    assert has_batchim("루멘")      # 멘=ㄴ받침
    assert not has_batchim("밴시")  # 시=받침없음
    assert not has_batchim("이그니스")  # 스=받침없음(ㅡ)
    assert has_batchim("타이양")    # 양=ㅇ받침
    assert not has_batchim("")
    assert not has_batchim("Red")   # 비한글

def test_rieul():
    assert is_rieul("태을")         # 을=ㄹ
    assert not is_rieul("루멘")

def test_variants_cons_to_vowel():
    v = dict(josa_variants("흑룡", "밴시"))  # 받침→모음
    assert v["흑룡을"] == "밴시를"
    assert v["흑룡이"] == "밴시가"
    assert v["흑룡은"] == "밴시는"
    assert v["흑룡과"] == "밴시와"
    assert v["흑룡으로"] == "밴시로"
    assert v["흑룡이라는"] == "밴시라는"
    assert v["흑룡이었"] == "밴시였"

def test_variants_vowel_to_cons():
    v = dict(josa_variants("만도", "미리안"))  # 모음→받침
    assert v["만도는"] == "미리안은"
    assert v["만도가"] == "미리안이"
    assert v["만도를"] == "미리안을"
    assert v["만도와"] == "미리안과"
    assert v["만도로"] == "미리안으로"

def test_variants_rieul_dir():
    v = dict(josa_variants("태을", "타이양"))  # ㄹ받침(로)→ㅇ받침(으로)
    assert v["태을로"] == "타이양으로"
    assert v["태을은"] == "타이양은"

def test_variants_same_ending_identity():
    v = dict(josa_variants("용맥", "루멘"))  # 둘 다 받침
    assert v["용맥을"] == "루멘을"
    assert v["용맥으로"] == "루멘으로"
```

- [ ] **Step 2: 실패 확인**

Run: `cd "D:/my_projects/OPUS-Echo-Of-Starsong-Korean" && python -m pytest tools/en2kr/tests/test_josa.py -q`
Expected: FAIL (ModuleNotFoundError: tools.en2kr.josa)

- [ ] **Step 3: 구현**

```python
# tools/en2kr/josa.py
"""한국어 조사(josa) 종성 인식 교정. 고유명 치환 시 앞말 종성 변화 반영."""

_HANGUL_BASE = 0xAC00
_HANGUL_LAST = 0xD7A3

def _last_hangul(word):
    for ch in reversed(word):
        if _HANGUL_BASE <= ord(ch) <= _HANGUL_LAST:
            return ch
    return None

def has_batchim(word):
    ch = _last_hangul(word)
    if ch is None:
        return False
    return (ord(ch) - _HANGUL_BASE) % 28 != 0

def is_rieul(word):
    ch = _last_hangul(word)
    if ch is None:
        return False
    return (ord(ch) - _HANGUL_BASE) % 28 == 8  # ㄹ

def _dir_form(word):
    # 받침 없음 또는 ㄹ받침 → '로', 그 외 → '으로'
    return "로" if (not has_batchim(word) or is_rieul(word)) else "으로"

# 추상 조사 → (받침형, 모음형). DIR은 특수 처리.
_PAIRS = [
    ("이라는", "라는"), ("이라고", "라고"), ("이란", "란"),
    ("이었", "였"), ("이에요", "예요"), ("이야", "야"), ("이다", "다"),
    ("이나", "나"), ("이며", "며"), ("이자", "자"),
    ("은", "는"), ("이", "가"), ("을", "를"), ("과", "와"),
]

def josa_variants(old, new):
    old_b, new_b = has_batchim(old), has_batchim(new)
    out = []
    for bat, vow in _PAIRS:
        o = bat if old_b else vow
        n = bat if new_b else vow
        out.append((old + o, new + n))
    # 방향격 으로/로 (ㄹ 특수)
    out.append((old + _dir_form(old), new + _dir_form(new)))
    # 최장 조사부터
    out.sort(key=lambda p: len(p[0]), reverse=True)
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tools/en2kr/tests/test_josa.py -q`
Expected: PASS (전 테스트)

- [ ] **Step 5: Commit**

```bash
git add tools/en2kr/josa.py tools/en2kr/tests/test_josa.py
git commit -q -m "feat(en2kr): josa 종성 교정 코어 + 테스트"
```

---

### Task 3: substitute.py — 맵 적용 엔진 (TDD)

**Files:**
- Create: `tools/en2kr/substitute.py`
- Test: `tools/en2kr/tests/test_substitute.py`

**Interfaces:**
- Consumes: `josa.josa_variants`
- Produces:
  - `build_rules(mapping: list[dict]) -> dict[str,str]` — 각 엔트리에서 표면형 규칙 생성. `type=="auto"`는 josa 변형 + bare, `type=="literal"`은 `pairs`의 정확 쌍만(bare/josa 확장 없음).
  - `apply_text(text: str, rules: dict[str,str]) -> str` — 단일 패스, 최장 우선, 비-캐스케이드 치환.
  - mapping 엔트리 스키마: `{"type":"auto","old":"흑룡","new":"밴시"}` 또는 `{"type":"literal","pairs":[["「곤」","「쿤」"],["태공선 「곤」","우주선 「쿤」"]]}`

- [ ] **Step 1: 실패 테스트 작성**

```python
# tools/en2kr/tests/test_substitute.py
from tools.en2kr.substitute import build_rules, apply_text

MAP = [
    {"type":"auto","old":"용맥","new":"루멘"},
    {"type":"auto","old":"흑룡","new":"밴시"},
    {"type":"auto","old":"만도","new":"미리안"},
    {"type":"literal","pairs":[["「곤」","「쿤」"]]},
]

def R():
    return build_rules(MAP)

def test_bare_and_josa():
    r = R()
    assert apply_text("용맥을 따라", r) == "루멘을 따라"
    assert apply_text("흑룡을 쫓는다", r) == "밴시를 쫓는다"
    assert apply_text("흑룡이 나타났다", r) == "밴시가 나타났다"
    assert apply_text("만도는 거대하다", r) == "미리안은 거대하다"

def test_no_cascade():
    # 용맥→루멘 후, '루멘'을 다시 건드리는 규칙 없음
    assert apply_text("용맥", R()) == "루멘"

def test_literal_only_exact():
    r = R()
    assert apply_text("태공선 「곤」에", r) == "태공선 「쿤」에"
    # 리터럴은 bare '곤' 미치환 (곤란 보호)
    assert apply_text("곤란한 상황", r) == "곤란한 상황"

def test_longest_match_first():
    r = R()
    # '흑룡이라는'이 '흑룡이'보다 먼저 매치
    assert apply_text("흑룡이라는 혜성", r) == "밴시라는 혜성"

def test_preserve_tags():
    r = R()
    s = "<color=#fff>용맥</color> {0} 「흑룡」"
    assert apply_text(s, r) == "<color=#fff>루멘</color> {0} 「밴시」"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tools/en2kr/tests/test_substitute.py -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 구현**

```python
# tools/en2kr/substitute.py
"""치환 맵 → 규칙 → 단일패스 적용."""
import re
from tools.en2kr.josa import josa_variants

def build_rules(mapping):
    rules = {}
    for e in mapping:
        if e["type"] == "auto":
            old, new = e["old"], e["new"]
            for o, n in josa_variants(old, new):
                rules.setdefault(o, n)
            rules.setdefault(old, new)  # bare (최후)
        elif e["type"] == "literal":
            for o, n in e["pairs"]:
                rules.setdefault(o, n)
        else:
            raise ValueError(f"unknown type: {e['type']}")
    return rules

def apply_text(text, rules):
    if not rules:
        return text
    # 최장 표면형 우선 정렬 → 알터네이션 순서 보장
    keys = sorted(rules.keys(), key=len, reverse=True)
    pattern = re.compile("|".join(re.escape(k) for k in keys))
    return pattern.sub(lambda m: rules[m.group(0)], text)
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tools/en2kr/tests/ -q`
Expected: PASS (josa + substitute 전체)

- [ ] **Step 5: Commit**

```bash
git add tools/en2kr/substitute.py tools/en2kr/tests/test_substitute.py
git commit -q -m "feat(en2kr): 단일패스 josa 인식 치환 엔진 + 테스트"
```

---

### Task 4: substitution_map.json — 세계관·신명·인물 확정 맵

**Files:**
- Create: `tools/en2kr/substitution_map.json`

spec §2-1~2-3, 2-5 기반. `auto`(distinctive·고빈도) + `literal`(substring 위험·문맥 제한).

- [ ] **Step 1: 맵 작성**

```json
[
  {"type":"auto","old":"용맥","new":"루멘"},
  {"type":"auto","old":"만도","new":"미리안"},
  {"type":"auto","old":"흑룡","new":"밴시"},
  {"type":"auto","old":"백룡","new":"피닉스"},
  {"type":"auto","old":"촉룡","new":"이그니스"},
  {"type":"auto","old":"후토","new":"테라"},
  {"type":"auto","old":"대황","new":"엑시디움"},
  {"type":"auto","old":"을황","new":"헬리우스"},
  {"type":"auto","old":"태을","new":"타이양"},
  {"type":"auto","old":"종손","new":"가주"},
  {"type":"auto","old":"홍루","new":"레드 챔버"},
  {"type":"auto","old":"영해","new":"동해"},
  {"type":"auto","old":"이현","new":"셴 리"},

  {"type":"literal","pairs":[
    ["용문","미리안 게이트"],
    ["「곤」","「쿤」"],
    ["태공선 「쿤」","우주선 「쿤」"],
    ["태공선 「곤」","우주선 「쿤」"],
    ["영해이씨","로댕 가문"],
    ["이씨 가문","로댕 가문"],
    ["연가 자객","카이트 자객"],
    ["영기 정련소","루미움 정련소"],
    ["영기 정련장","루미움 정련장"],
    ["영기 궤도포","루미움 궤도포"],
    ["영기 환적소","루미움 환적소"],
    ["영기 연구 센터","루미움 연구 센터"],
    ["영기 가공","루미움 가공"],
    ["조가 영기","조가 루미움"],
    ["영수 가공 공장","루메나이드 가공 공장"],
    ["영수 가공","루메나이드 가공"]
  ]}
]
```

> 주의: `이현→셴 리`, `홍루→레드 챔버`는 auto지만 결과에 공백 포함 — josa는 마지막 음절(리/버) 기준으로 정상 처리됨. `太乙(타이양)`의 태양신 호칭 "태양신"은 유지(별도 미치환).
> `汜(영)` 단독·기타 문맥은 이 맵에 없음 → Task 7 LLM 패스.

- [ ] **Step 2: 스키마 검증**

```bash
python - <<'PY'
import json
m=json.load(open("tools/en2kr/substitution_map.json",encoding="utf-8"))
for e in m:
    assert e["type"] in ("auto","literal")
    if e["type"]=="auto": assert e["old"] and e["new"]
    else:
        for o,n in e["pairs"]: assert o and n
print("map ok:", len(m), "entries")
PY
```
Expected: `map ok: N entries`

- [ ] **Step 3: Commit**

```bash
git add tools/en2kr/substitution_map.json
git commit -q -m "feat(en2kr): 세계관·신명·인물 확정 치환 맵"
```

---

### Task 5: places_map.json — glossary 지명 「」 리터럴 생성

**Files:**
- Create: `tools/en2kr/gen_places_map.py`
- Create(생성): `tools/en2kr/places_map.json`

spec §2-4. glossary 지명(114) + crosslang places의 KR을, EN 개명 대응표로 「」-delimited 리터럴 치환 생성. 「」 경계라 substring 안전.

- [ ] **Step 1: 지명 개명 대응표 + 생성기 작성**

`RENAME`은 spec §2-4의 「」 내부 토큰 단위(한자음/구KR → EN). glossary의 KR 지명 문자열에서 「」 안 토큰만 치환.

```python
# tools/en2kr/gen_places_map.py
"""glossary 지명 KR → EN 개명 「」 리터럴 치환쌍 생성."""
import json, re

# 「」 내부 토큰 개명표 (구KR → EN). spec §2-4.
RENAME = {
    "조산": "마운트 오로라", "철람성": "아이언윈드",
    "주홍": "버밀리온", "다랑": "루시두스", "각신": "콘카",
    "지신": "사라스와티", "귀인": "포르투나", "해국": "오세아니아",
    "대황": "엑시디움", "촉룡": "이그니스", "후토": "테라", "을황": "헬리우스",
    "부생약몽": "드리머스 핏", "대분종": "그레이트 벤저민",
    "서뢰산": "웨스트 마운트 토니트루스", "보제": "파이제", "백제": "파이제",
    "어와": "에르바", "호사": "후샤", "촉룡 1호": "이그니스 1호",
}
# 철람N(광산 번호): 철람24→아이언윈드24
NUM_RENAME = ("철람", "아이언윈드")

def bracket_tokens(kr_place):
    return re.findall(r"「([^」]+)」", kr_place)

def convert_token(tok):
    if tok in RENAME:
        return RENAME[tok]
    m = re.fullmatch(NUM_RENAME[0] + r"(\d+)", tok)
    if m:
        return NUM_RENAME[1] + m.group(1)
    # 한자 병기 제거형: "조산(朝山)" → "마운트 오로라"
    base = re.sub(r"\([^)]*\)", "", tok)
    if base in RENAME:
        return RENAME[base]
    return None  # 변경 없음(이미 EN 음차/의역/병음)

def main():
    g = json.load(open("Trans-EN2KR/glossary/master_glossary.json", encoding="utf-8"))
    places = g.get("places") or []
    # master_glossary.json 구조 확인 후 지명 리스트 경로 조정 (아래 Step 2에서 검증)
    pairs = []
    seen = set()
    for p in places:
        kr = p["kr"] if isinstance(p, dict) else p
        for tok in bracket_tokens(kr):
            nt = convert_token(tok)
            if nt and f"「{tok}」" not in seen:
                pairs.append([f"「{tok}」", f"「{nt}」"])
                seen.add(f"「{tok}」")
    json.dump([{"type":"literal","pairs":pairs}],
              open("tools/en2kr/places_map.json","w",encoding="utf-8"),
              ensure_ascii=False, indent=1)
    print("places pairs:", len(pairs))

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: master_glossary.json 지명 구조 확인 후 생성**

먼저 실제 구조 확인(지명 배열 키):
```bash
python -c "import json;d=json.load(open('Trans-EN2KR/glossary/master_glossary.json',encoding='utf-8'));print(list(d.keys()) if isinstance(d,dict) else type(d))"
```
출력의 지명 배열 키에 맞춰 `gen_places_map.py`의 `g.get('places')`와 `p['kr']` 경로를 수정(예: `d["places"]`가 `{원어:KR}` dict면 `for kr in places.values()`). 그 후:
```bash
python tools/en2kr/gen_places_map.py
python -c "import json;print(json.load(open('tools/en2kr/places_map.json',encoding='utf-8'))[0]['pairs'][:8])"
```
Expected: `places pairs: N` (N≥15), 샘플에 `["「조산(朝山)」","「마운트 오로라」"]` 류 포함.

- [ ] **Step 3: 스팟 검증** — 생성된 쌍이 spec §2-4와 일치하는지 눈으로 확인(조산·철람성·귀인·해국·대황·부생약몽 등 존재). 누락 지명은 `RENAME`에 추가 후 재생성.

- [ ] **Step 4: Commit**

```bash
git add tools/en2kr/gen_places_map.py tools/en2kr/places_map.json
git commit -q -m "feat(en2kr): glossary 지명 「」 리터럴 치환 생성"
```

---

### Task 6: apply.py — 번역·문서 파일 변환

**Files:**
- Create: `tools/en2kr/apply.py`
- Modify(변환): `Trans-EN2KR/translations/*.json`, `Trans-EN2KR/characters/character_profiles.md`, `Trans-EN2KR/characters/character_personas.md`

**Interfaces:**
- Consumes: `substitute.build_rules/apply_text`, `substitution_map.json`, `places_map.json`
- JSON: 문자열 **값만** 재귀 변환(키 불변). MD: 전체 텍스트 변환.

- [ ] **Step 1: apply.py 작성**

```python
# tools/en2kr/apply.py
"""EN2KR 치환 적용. JSON은 값만, MD는 전체."""
import json, sys, glob
from tools.en2kr.substitute import build_rules, apply_text

def load_rules():
    mapping = json.load(open("tools/en2kr/substitution_map.json", encoding="utf-8"))
    mapping += json.load(open("tools/en2kr/places_map.json", encoding="utf-8"))
    return build_rules(mapping)

def walk(obj, rules):
    if isinstance(obj, str):
        return apply_text(obj, rules)
    if isinstance(obj, list):
        return [walk(x, rules) for x in obj]
    if isinstance(obj, dict):
        return {k: walk(v, rules) for k, v in obj.items()}  # 키 불변
    return obj

def convert_json(path, rules):
    data = json.load(open(path, encoding="utf-8"))
    out = walk(data, rules)
    json.dump(out, open(path, "w", encoding="utf-8"), ensure_ascii=False, indent=1)

def convert_md(path, rules):
    t = open(path, encoding="utf-8").read()
    open(path, "w", encoding="utf-8").write(apply_text(t, rules))

def main():
    rules = load_rules()
    for f in glob.glob("Trans-EN2KR/translations/*.json"):
        convert_json(f, rules); print("json:", f)
    for f in ["Trans-EN2KR/characters/character_profiles.md",
              "Trans-EN2KR/characters/character_personas.md"]:
        convert_md(f, rules); print("md:", f)

if __name__ == "__main__":
    main()
```

> `uid2speaker.json`/`actor_defs.json`은 CHT·actor키 기반이라 변환 대상 제외(무변경).

- [ ] **Step 2: 변환 실행**

```bash
cd "D:/my_projects/OPUS-Echo-Of-Starsong-Korean" && python -m tools.en2kr.apply
```
Expected: translations 10개 + md 2개 처리 로그.

- [ ] **Step 3: JSON 유효성 + indent 원형 확인**

```bash
python - <<'PY'
import json,glob
for f in glob.glob("Trans-EN2KR/translations/*.json"):
    json.load(open(f,encoding="utf-8"))
print("all json valid")
PY
git diff --stat Trans-EN2KR/translations | tail -5
```
Expected: `all json valid`, diff에 12개 파일 변경.

- [ ] **Step 4: 확정 용어 잔여 스캔 (0이어야 함, 汜/영 제외)**

```bash
python - <<'PY'
import glob,re
terms = ["용맥","만도","흑룡","백룡","촉룡","후토","대황","을황","태을",
         "종손","홍루","영해","이현","용문","이씨 가문","연가 자객"]
bad=0
for f in glob.glob("Trans-EN2KR/translations/*.json"):
    t=open(f,encoding="utf-8").read()
    for w in terms:
        c=t.count(w)
        if c: print(f"{f}: {w} x{c}"); bad+=c
print("residual:", bad)
PY
```
Expected: `residual: 0`. 0 아니면 해당 라인 조사 → 복합어/문맥 문제면 Task 7로, 맵 누락이면 맵 보강 후 재적용(git으로 되돌린 뒤).

> 재적용 시 반드시 `git checkout -- Trans-EN2KR/translations Trans-EN2KR/characters/*.md` 로 원복 후 재실행(중복 치환 방지).

- [ ] **Step 5: Commit**

```bash
git add Trans-EN2KR/translations Trans-EN2KR/characters/character_profiles.md Trans-EN2KR/characters/character_personas.md
git commit -q -m "feat(en2kr): 확정 고유명사 결정론적 치환 적용"
```

---

### Task 7: 汜(영)→루멘 계열 + 복합·잔여 LLM 클린업 패스

**Files:**
- Modify: `Trans-EN2KR/translations/*.json` (후보 라인만)

결정론 불가 항목: 단독 `영`(汜=루멘 vs 靈=영혼 등), `영기/영수/영정` 미시설 문맥, 복합 지명(남조산 등), josa 엣지. 문맥 판별 필요 → LLM.

- [ ] **Step 1: 후보 라인 추출**

```bash
python - <<'PY'
import json,glob,re
CAND=re.compile(r"영기|영수|영정|(?<![가-힣])영(?![가-힣])|조산|철람|해국|주홍|다랑|각신|지신|귀인|부생|대분종|서뢰")
rows=[]
for f in glob.glob("Trans-EN2KR/translations/*.json"):
    d=json.load(open(f,encoding="utf-8"))
    def scan(k,v):
        if isinstance(v,str) and CAND.search(v): rows.append((f,k,v))
        elif isinstance(v,dict):
            for kk,vv in v.items(): scan(kk,vv)
    for k,v in d.items(): scan(k,v)
json.dump(rows, open("tools/en2kr/_candidates.json","w",encoding="utf-8"), ensure_ascii=False, indent=1)
print("candidates:", len(rows))
PY
```
Expected: `candidates: N` (수십~수백).

- [ ] **Step 2: 문맥 분류·치환 (LLM/에이전트)**

`_candidates.json` 각 라인에 대해 spec §2 규칙으로 판별·치환:
- `汜` 의미(물질·에테르·"빛의 바다에 녹아듦"·감정 매질) → `영/영기/영수/영정` = **루멘/루미움/루메나이드/루메나이트** (josa 교정).
- `靈` 의미(영혼·영원·영역·환영·촬영 등) → **불변**.
- 복합 지명(남조산=사우스 마운트 오로라 등)·잔여 지명 → spec §2-4 EN.
- 결과를 `(file, key, new_value)`로 수집. **에이전트 사용 권장**: dispatching-parallel-agents로 파일별 분할, 각 에이전트에 spec §2 + 규칙 전달, 구조화 출력(JSON) 반환.

- [ ] **Step 3: 치환 적용 (키 매칭, 값 교체)**

```python
# 에이전트 산출 patches.json: [[file,key,newvalue],...]
python - <<'PY'
import json
patches=json.load(open("tools/en2kr/_patches.json",encoding="utf-8"))
byfile={}
for f,k,v in patches: byfile.setdefault(f,{})[k]=v
for f,kv in byfile.items():
    d=json.load(open(f,encoding="utf-8"))
    def setk(d):
        for k,val in list(d.items()):
            if k in kv: d[k]=kv[k]
            elif isinstance(val,dict): setk(val)
    setk(d)
    json.dump(d,open(f,"w",encoding="utf-8"),ensure_ascii=False,indent=1)
print("patched files:", len(byfile))
PY
```

- [ ] **Step 4: 검증** — 靈(영혼 등) 오치환 없는지 스팟체크, 잔여 `영기/영수/영정`(汜) 0, 복합 지명 정리 확인.

```bash
python -c "import glob;print(sum(open(f,encoding='utf-8').read().count('영기') for f in glob.glob('Trans-EN2KR/translations/*.json')))"
```
Expected: `0` (또는 靈氣 정당 잔여만 — 확인).

- [ ] **Step 5: Commit**

```bash
git add Trans-EN2KR/translations
git commit -q -m "feat(en2kr): 汜(영)→루멘 계열 및 복합 지명 문맥 클린업"
```

---

### Task 8: glossary/decisions/README/crosslang 재작성

**Files:**
- Modify: `Trans-EN2KR/glossary/master_glossary.{json,md}`, `Trans-EN2KR/glossary/proper_noun_crosslang.{json,md}`, `Trans-EN2KR/decisions/localization_decisions.md`, `Trans-EN2KR/README.md`

단일 권위 문서를 EN2KR 원칙으로 갱신. spec §0~§2가 내용 소스.

- [ ] **Step 1: crosslang KR 열 갱신** — `proper_noun_crosslang.json`의 각 엔트리 `kr`을 EN2KR 값으로. `apply.py`의 rules로 1차 자동 변환 후, 신명·지명은 spec §2와 대조해 수기 보정(태을→타이양, 을황→헬리우스, 촉룡→이그니스, 조산→마운트 오로라 …). `.md`는 `.json`에서 재생성 또는 동일 규칙 적용.

- [ ] **Step 2: master_glossary 갱신** — 세계관/신명/인물/지명/세력 표를 EN2KR로. "고유명사 예외=한자음" 서술을 "고유명사=영문판 수용"으로 교체. 汜 물질군: 루멘/루미움/루메나이드/루메나이트.

- [ ] **Step 3: decisions 재작성** — §0 대원칙을 "영문 원문 우선"으로. §1/§2-2/§9/§10/§11의 "한자음 유지" 결정을 EN 수용으로 뒤집고 근거를 spec 참조로 기술. Complex와의 차이(무엇을 왜 뒤집었는지) 명시.

- [ ] **Step 4: README 갱신** — 첫 문단을 "영어(EN)를 고유명 기준으로 우선" 설명으로. 핵심 결정 빠른 참조(§37~44 해당)를 EN2KR 값으로 교체(루멘·별노래·메아리·밴시·미리안·헬리우스·타이양·이그니스·테라·엑시디움·가주·로댕 가문 등).

- [ ] **Step 5: 문서 JSON 유효성 + 상호 일치 확인**

```bash
python -c "import json;json.load(open('Trans-EN2KR/glossary/master_glossary.json',encoding='utf-8'));json.load(open('Trans-EN2KR/glossary/proper_noun_crosslang.json',encoding='utf-8'));print('doc json ok')"
grep -l "용맥\|한자음 유지\|을황\|촉룡" Trans-EN2KR/glossary Trans-EN2KR/decisions Trans-EN2KR/README.md -r || echo "no stale terms"
```
Expected: `doc json ok`; stale 용어 없음(설명상 인용 제외).

- [ ] **Step 6: Commit**

```bash
git add Trans-EN2KR/glossary Trans-EN2KR/decisions Trans-EN2KR/README.md
git commit -q -m "docs(en2kr): 용어집·결정·README를 영문 우선 기준으로 재작성"
```

---

### Task 9: 최종 검증 (verify.py)

**Files:**
- Create: `tools/en2kr/verify.py`

- [ ] **Step 1: verify.py 작성 + 실행**

```python
# tools/en2kr/verify.py
"""EN2KR 최종 검증: JSON유효성·플레이스홀더보존·잔여용어·구조일치."""
import json, glob, re

def json_valid():
    for f in glob.glob("Trans-EN2KR/**/*.json", recursive=True):
        json.load(open(f, encoding="utf-8"))
    print("[ok] all json valid")

def residual():
    terms=["용맥","만도","흑룡","백룡","촉룡","후토","대황","을황","태을",
           "종손","홍루","이현","용문","영기","영수","영정"]
    bad=0
    for f in glob.glob("Trans-EN2KR/translations/*.json"):
        t=open(f,encoding="utf-8").read()
        for w in terms:
            c=t.count(w)
            if c: print(f"[warn] {f}: {w} x{c}"); bad+=c
    print("[ok] residual=0" if not bad else f"[FAIL] residual={bad}")

def placeholders():
    # 치환 전후 태그 수 보존은 Complex2KR 대비 개수 비교
    pat=re.compile(r"\{\d+\}|@\{\d+\}@|%s|<[^>]+>|「|」")
    for f in glob.glob("Trans-EN2KR/translations/*.json"):
        b=os.path.basename(f)
        a=len(pat.findall(open(f,encoding="utf-8").read()))
        c=len(pat.findall(open(f"Trans-Complex2KR/translations/{b}",encoding="utf-8").read()))
        flag="" if a==c else "  <-- MISMATCH"
        print(f"[tags] {b}: en2kr={a} complex={c}{flag}")

import os
if __name__=="__main__":
    json_valid(); residual(); placeholders()
```

```bash
python tools/en2kr/verify.py
```
Expected: `[ok] all json valid`, `[ok] residual=0`, 태그 개수 EN2KR==Complex(MISMATCH 없음 — 「」는 치환으로 불변, 태그 불변).

- [ ] **Step 2: 대사 스팟체크** — story_kr.json에서 주요 화자 대사 10개 표본 추출해 josa·자연성 육안 확인(밴시가/를/는, 루멘을, 미리안은, 가주 어른 등).

```bash
python - <<'PY'
import json
d=json.load(open("Trans-EN2KR/translations/story_kr.json",encoding="utf-8"))
import itertools
for k,v in itertools.islice(((k,v) for k,v in d.items() if any(w in v for w in ["루멘","밴시","미리안","이그니스","헬리우스","타이양","가주"])),10):
    print(k[:8], v[:80])
PY
```
Expected: 치환어가 josa와 자연스럽게 결합된 대사 표본.

- [ ] **Step 3: Commit**

```bash
git add tools/en2kr/verify.py
git commit -q -m "test(en2kr): 최종 검증 스크립트(JSON·잔여·태그보존)"
```

---

## Self-Review 결과

- **Spec 커버리지**: §1 구조=Task1·8, §2-1~2-5 치환=Task4·5·6·7, §3 실행방식=Task2·3·6·7, §4 검증=Task6·9. 전 항목 매핑됨.
- **잔여 위험**: Task7 LLM 패스는 비결정론 — 검증(Task9 residual/스팟)으로 게이트. `汜氣(靈氣)` 정당 잔여 가능성은 Task7-4/Task9에서 육안 확인.
- **타입 일관성**: `build_rules`/`apply_text`/`josa_variants` 시그니처 Task2·3·6에서 일치. 맵 스키마(`type/old/new/pairs`) Task3·4·5·6 일치.
